[
  "If cci_10 is elevated (current=-160.3755), the model estimate for long probability typically increases.",
  "If ret_1_lag_52 is elevated (current=0.0048), the model estimate for long probability typically decreases.",
  "If volume_lag_42 is elevated (current=3.5203), the model estimate for long probability typically decreases.",
  "If volume_lag_42 is elevated (current=4.7240), the model estimate for long probability typically decreases.",
  "If volume_lag_42 is elevated (current=18.8612), the model estimate for long probability typically increases.",
  "If volume_lag_42 is elevated (current=13.8064), the model estimate for long probability typically increases.",
  "If volume_lag_42 is elevated (current=17.0496), the model estimate for long probability typically increases.",
  "If ret_1_lag_52 is elevated (current=0.0018), the model estimate for long probability typically decreases.",
  "If ret_1_lag_40 is elevated (current=0.0013), the model estimate for long probability typically increases.",
  "If volume_lag_42 is elevated (current=2.5781), the model estimate for long probability typically decreases.",
  "If volume_lag_3 is elevated (current=1.4679), the model estimate for long probability typically increases.",
  "If mfi_8 is elevated (current=95.0747), the model estimate for long probability typically increases.",
  "If volume_lag_3 is elevated (current=1.9648), the model estimate for long probability typically increases.",
  "If volume_lag_3 is elevated (current=1.8101), the model estimate for long probability typically increases.",
  "If volume_lag_20 is elevated (current=84.1561), the model estimate for long probability typically increases.",
  "If volume_lag_3 is elevated (current=2.3657), the model estimate for long probability typically increases.",
  "If volume_lag_26 is elevated (current=2.6926), the model estimate for long probability typically decreases.",
  "If volume_lag_23 is elevated (current=90.8113), the model estimate for long probability typically decreases.",
  "If volume_lag_11 is elevated (current=2.3090), the model estimate for long probability typically increases.",
  "If volume_lag_7 is elevated (current=2.9021), the model estimate for long probability typically decreases.",
  "If eth_vol_chg_1 is elevated (current=-0.6048), the model estimate for long probability typically increases.",
  "If volume_lag_20 is elevated (current=130.2940), the model estimate for long probability typically increases.",
  "If volume_lag_3 is elevated (current=1.3040), the model estimate for long probability typically increases.",
  "If volume_lag_26 is elevated (current=3.0266), the model estimate for long probability typically decreases.",
  "If volume_lag_26 is elevated (current=5.9139), the model estimate for long probability typically decreases.",
  "If volume_lag_11 is elevated (current=0.7787), the model estimate for long probability typically increases."
]