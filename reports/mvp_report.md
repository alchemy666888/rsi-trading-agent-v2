# Archived Report

This file is retained only as a historical artifact from the original MVP prototype.

It is **not** the current source of truth for repository behavior or benchmark quality.

Current runs now write versioned artifact bundles under `artifacts/<run_id>/`, including:
- held-out simulation metrics
- purged walk-forward benchmark metrics
- optimization provenance
- trade and risk artifacts
