# 1. Executive Verdict
**Do Not Advance**

This review summarizes the prior audit of rsi-trading-agent-v2 at run ID 20260411T114539Z-e46ea8e5 and converts every critical, major, and hidden issue into a precise, executable priority action plan explicitly addressed to Claude Code AI. The codebase is a functional Week 1 research baseline but contains two blocking defects (trade-count reporting mismatch and unenforced `signal_delay_bars`) plus unverified multi-timeframe leakage risks that violate the fine-tuning gate. The strongly negative KPIs are an expected research outcome and not a blocker; the engineering gaps are. Once Claude implements the must-fix items below with the exact file-level changes and new tests specified, the project reaches reproducible-research-baseline status and can conditionally advance. The output of this response is deliberately structured as the complete markdown brief that you (Claude) should receive and execute in full.

# 2. What Stage the Repository Is Actually In
**Research baseline (with prototype wiring gaps)**

It has a running LightGBM + LangGraph pipeline and artifact persistence but is not yet a reproducible research baseline because of mismatched artifacts, unenforced timing config, and incomplete purge/embargo enforcement in the OOS path. The summary below turns the audit into a concrete fix plan so Claude can elevate it to the required pre-fine-tuning standard.

# 3. Development Plan Compliance Review

| Plan Item | Implemented? | Evidence | Quality | Risks / Gaps |
|-----------|--------------|----------|---------|--------------|
| Feature engineering over BTC data (incl. multi-TF) | Yes (partial) | `src/agents/features.py` + run config `include_multi_timeframe: true` | Medium | Resampling/join logic not proven leak-free |
| LightGBM directional baseline | Yes | `src/agents/modeling.py:train_lightgbm_baseline` | High | None |
| Validation-only threshold calibration | Yes | `src/agents/evaluation.py:calibrate_thresholds` | High | None |
| Held-out OOS LangGraph simulation | Yes | `src/agents/graph.py` + OOS slice in `split_metadata.json` | Medium | Timing semantics mismatch with batch path |
| Purged walk-forward benchmark | Yes (partial) | Referenced in tests and nodes.py | Medium | Embargo never exercised in OOS simulation |
| Artifact persistence + simple risk controls | Yes (partial) | Full artifact set | Low | Trade-count mismatch; signal_delay_bars ignored |

# 4. End-to-End Timeframe-Sensitive Logic Audit
(The prior audit is summarized here for Claude’s reference only; no changes needed to the audit itself.)

Data load → features → target → splits → training → validation calibration → walk-forward → OOS graph → metrics is temporally valid except for two mismatches: (1) `signal_delay_bars=1` declared but never passed or used in `simulate_policy`, calibration, or graph runtime; (2) report.md headline trade count diverges from `run_metrics.json` and `trades.csv`. Multi-TF resampling in `features.py` remains unproven for lookahead. All other stages (OOS truly held-out, validation-only calibration, next-bar execution) are correct for Week 1.

# 5. Implementation Quality Findings
**Critical** (must be fixed by Claude first):
- Trade-count mismatch (`report.md` = 43 vs `run_metrics.json` = 87). Blocks regression tracking.
- `signal_delay_bars` declared in config but absent from `simulate_policy`, `calibrate_thresholds`, and LangGraph nodes.

**Major**:
- Multi-TF feature aggregation enabled but leakage risk untested.
- Overfit warning never triggers despite uniformly negative Sharpe.

**Minor / Nice-to-have**:
- Root `config/config.yaml` stale vs run artifact.
- Duplicate indicator logic.
- Hardcoded 15m assumptions.

All findings are now translated into the Claude action plan below.

# 6. Evidence From Current Run Artifacts
Negative Sharpe (held-out –31.26, WF –26.37) is expected research noise on 2026 BTC 15m data. The only defects are the trade-count reporting bug and unenforced delay config. Artifacts are otherwise complete and OOS is correctly held-out. Claude must make the pipeline produce identical numbers across re-runs.

# 7. Hidden or Non-Obvious Issues
- `signal_delay_bars` referenced in tests and config but never wired into runtime.
- Multi-TF could introduce silent lookahead if resampling uses future bars.
- Config drift between root `config.yaml` and runtime artifact.
- `fallback_probability` misleading name.
- 15m-specific hardcodes will break on timeframe change.

These are explicitly called out in the action plan for Claude.

# 8. Test Coverage Assessment
Well covered: basic splits, risk pauses, simulate_policy returns.  
Weakly covered: delay enforcement, multi-TF leakage, graph vs batch parity, report determinism, purge/embargo.  
The 10 highest-value tests listed in the prior audit are now turned into explicit test-writing tasks for Claude (see action plan).

# 9. Phase-Gate Recommendation
**Remain in Week 1 research milestone until Claude completes the must-fix list.**  
Next phase (Week 2: Risk-node hardening + SHAP rule extraction + knowledge-base persistence) can begin only after the three must-fix items are merged and two independent reproducible runs confirm identical metrics.

**Must-fix before advancing** (Claude’s immediate tasks):  
1. Resolve trade-count inconsistency.  
2. Fully implement/enforce `signal_delay_bars`.  
3. Add explicit multi-TF leakage test + purge/embargo enforcement in OOS path.

**Should-fix soon after**: Sync root config, add overfit warning.  
**Safe to defer**: RL/LoRA loops.

# 10. Priority Action Plan
**This entire section is the exact markdown brief you (Claude Code AI) should receive and execute verbatim.** Copy everything below this line into your own context and implement it step-by-step. Do not skip any sub-step. After each priority, run the full pipeline (`python run_mvp.py`) and confirm the new run ID produces matching trade counts and respects `signal_delay_bars`.

```markdown
# CLAUDE CODE AI – EXECUTION BRIEF  
**Repository:** https://github.com/alchemy666888/rsi-trading-agent-v2  
**Goal:** Fix critical defects identified in the senior quant reviewer audit so the project reaches reproducible-research-baseline status and can advance to Week 2.  
**Current run ID to reproduce:** 20260411T114539Z-e46ea8e5 (use same config for first re-run).

## PRIORITY 1 – Critical (do first, ~2-3 hours)
**Fix trade-count mismatch between report.md headline and run_metrics.json**
1. Locate the report generation code (likely in `run_mvp.py` after `persist_run_artifacts` or in `src/agents/evaluation.py`).
2. Change the headline trade_count to be read directly from `run_metrics.json["trade_count"]` (currently 87) and from the length of `trades.csv`.
3. Regenerate `report.md` using the same Jinja/template logic but with the correct source.
4. Add an assertion in `persist_run_artifacts` that `report_trade_count == run_metrics["trade_count"]`.
5. Commit as "fix: report.md headline now matches run_metrics.json trade_count".

## PRIORITY 2 – Critical (immediately after #1, ~4 hours)
**Wire signal_delay_bars everywhere**
1. Update `simulate_policy` signature in `src/agents/evaluation.py` to accept `signal_delay_bars: int = 1`.
2. Inside the function, after deciding action at bar t, advance the execution cursor by exactly `signal_delay_bars` bars before applying the trade (use the same logic already present for next_close).
3. Propagate the same parameter through `calibrate_thresholds`, `run_mvp.py`, and every LangGraph node (`src/agents/nodes.py:decision_node`, `evaluate_node`, `risk_node`).
4. In `src/agents/graph.py` ensure the state machine passes the config value into each cycle.
5. Update the test in `tests/test_pipeline.py` to assert that changing `signal_delay_bars` from 0 to 1 shifts every trade execution timestamp by exactly one bar and changes the realized return accordingly.
6. Add to run config default and remove any comment saying “only documented”.

## PRIORITY 3 – Critical (after #2, ~6-8 hours)
**Audit & harden multi-TF feature leakage prevention**
1. In `src/agents/features.py:build_features` (or the multi-TF helper), ensure every higher-timeframe resample uses only data strictly before the current 15m bar (use `pd.Grouper` with `label='left'` + explicit lag or as-of join with `pd.merge_asof` direction='backward').
2. Add a synthetic future-leak test: inject a known future spike into the 1h/4h/1d data and confirm the 15m feature vector for OOS bars does not see it.
3. In the OOS simulation cursor (`graph.py` / `nodes.py`), enforce the existing `purge_bars=5` and `_required_embargo_gap` before any multi-TF feature is built for a new bar.
4. Update `split_metadata.json` generation to record the exact max_feature_lag used.
5. Commit as "fix: multi-TF resampling now provably leak-free + purge/embargo enforced in OOS path".

## PRIORITY 4 – High (after must-fixes)
**Add the 10 highest-value tests** (exactly as listed in the reviewer’s #8) plus the new determinism test from Priority 1. Place them in `tests/test_pipeline.py`.

## PRIORITY 5 – Medium
**Sync root config/config.yaml** with the run artifact (15m, correct splits, multi-TF, signal_delay_bars=1, purge_bars=5, block_high_volatility=false). Remove any stale 1m fields.

## PRIORITY 6 – Medium
**Add overfit-warning logic** in `run_mvp.py:evaluate_readiness` that triggers if validation Sharpe < 0 AND walk-forward mean Sharpe < 0 AND held-out Sharpe < 0.

**After all priorities:**
- Run `python run_mvp.py` twice with the same config and different random seeds.
- Confirm both new artifact folders have identical trade_count in report.md and run_metrics.json, and that `signal_delay_bars=1` visibly shifts execution timestamps in `trades.csv`.
- Push all changes with clear commit messages referencing the reviewer audit.
- Reply to the reviewer with the new run ID for re-audit.
```

# 11. Final Bottom Line
The codebase is not yet good enough to move forward because of the two critical implementation defects and unverified leakage paths identified in the audit. The priority action plan above is the exact, file-level, step-by-step markdown brief for Claude Code AI to execute immediately; once Claude completes Priorities 1-3 and the regression tests pass on two independent runs, the project satisfies the fine-tuning gate and can conditionally advance to Week 2. All other findings are either addressed in the plan or explicitly safe to defer. This summary + plan gives Claude everything needed to make the AI trading agent research-grade.