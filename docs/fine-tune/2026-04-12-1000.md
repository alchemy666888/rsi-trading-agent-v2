# 1. Executive Verdict
**Do Not Advance**

The repository implements the Week 1 research milestone skeleton but contains material engineering defects that violate the phase-gate criteria. Signal-delay semantics are inconsistently applied between the batch calibration simulator (`evaluation.py:simulate_policy`) and the LangGraph runtime (`nodes.py:evaluate_node` + `graph.py`), creating a timing mismatch that invalidates both held-out simulation integrity and threshold calibration. Run artifacts for the exact run_id `20260412T013702Z-d73c98e2` are absent from the GitHub repository (404 on all `artifacts/<run_id>/*` paths), preventing end-to-end traceability of report.md, run_metrics.json, benchmark_metrics.json, etc. Trade-count inconsistency (report headline 43 vs. run_metrics 87) cannot be verified. These are not research underperformance issues but implementation defects that breach the “reproducible held-out and walk-forward metrics” and “persisted artifacts” requirements explicitly stated in docs/development-plan.md. The project must remain in the current phase.

# 2. What Stage the Repository Is Actually In
**Prototype wiring only (with critical simulation-timing gaps).**  
It is not a reproducible research baseline. Model training, feature engineering, and LangGraph wiring exist, but the OOS simulation does not faithfully replicate the calibration logic used for threshold selection. Artifact persistence is local-only and not committed for the assessed run_id, violating the fine-tuning gate’s “stable snapshot-based runs” and “persisted artifacts across multiple historical windows” prerequisites. Walk-forward benchmark code exists in tests but is not exercised in the main run_mvp.py path for this execution.

# 3. Development Plan Compliance Review

| Plan Item                                      | Implemented? | Evidence                                                                 | Quality | Risks / Gaps |
|-----------------------------------------------|--------------|--------------------------------------------------------------------------|---------|--------------|
| Feature engineering over BTC data             | Yes         | `features.py:compute_indicator_bundle` + multi-timeframe config          | Partial | Multi-TF aggregation logic only partially traced; potential backward-join leakage unverified due to file truncation. Plan mentions 1m data; config/run uses 15m. |
| LightGBM directional baseline                 | Yes         | `modeling.py:train_lightgbm_baseline` + `predict_probability`            | Good    | None material. |
| Validation-only threshold calibration         | Yes         | `evaluation.py:calibrate_thresholds` (Optuna)                           | Good    | None. |
| Held-out OOS LangGraph simulation             | Yes         | `run_mvp.py` → `graph.py:build_agent_graph` → OOS cursor loop            | Poor    | Timing mismatch vs. calibration (signal_delay_bars). |
| Purged walk-forward benchmark                 | Partial     | `evaluation.py:build_walk_forward_folds` (in tests)                     | Weak    | Not executed in main run_mvp.py path for this run_id; no persisted benchmark_metrics.json. |
| Artifact persistence & simple simulation risk controls | Partial     | `run_mvp.py:persist_run_artifacts` + risk_node                           | Poor    | Artifacts/<run_id> folder not present in repo → cannot reproduce. |
| NOT claiming self-improving loops / RL / LoRA / RAG / live trading | Yes         | No such code present                                                     | Good    | None. |

# 4. End-to-End Timeframe-Sensitive Logic Audit
- **Data load** (`data.py:load_historical_data`): exchange fetch or snapshot → full sorted DataFrame. Intended: causal OHLCV. Actual: causal. Valid.
- **Feature build** (`data.py` calls `features.py:build_features` on full DF before split): Intended: past-only rolling + multi-TF. Actual: talib rolling + presumed as-of joins. Temporally valid assuming no forward-looking ops (multi-TF code partially traced; backward joins would leak if present).
- **Target label** (`features.py` adds `target_up` – inferred as `close.shift(-1) > close` per plan and modeling usage): Intended: next-bar directional label. Actual: applied on full DF before slicing. Valid for supervised label (features remain causal).
- **Split construction** (`data.py:compute_split_metadata`): train/val ratios + `oos_start = validation_end + max(purge_bars, max_lag_bars+1)`. Intended: embargoed OOS. Actual: enforced. Valid.
- **Model training** (`modeling.py:train_lightgbm_baseline`): slice train only. Valid, no leakage.
- **Validation calibration** (`evaluation.py:calibrate_thresholds` → `simulate_policy`): uses `signal_delay_bars=1`, schedules actions at `idx + delay_bars`, applies to future bar. Valid within batch sim.
- **Walk-forward folds** (`evaluation.py:build_walk_forward_folds`): purge gaps present in test code. Not run in main path → unverified.
- **OOS graph execution** (`graph.py` → `nodes.py:data_node/predict_node/risk_node/decision_node/evaluate_node`): cursor advances bar-by-bar; `evaluate_node` applies current-cycle `target_position` to immediate next-bar return **without** delay scheduling. **Discrepancy**: calibration uses delay; runtime does not. Prediction claims “for next execution bar” but semantics mismatch.
- **Metrics / artifacts** (`evaluation.py:compute_run_metrics` + `persist_run_artifacts`): annualization correct; funding only in graph (absent in batch sim). Artifacts not committed for this run_id.

# 5. Implementation Quality Findings
**Critical**  
- Signal-delay mismatch: `evaluation.py:simulate_policy` (lines ~120-160) implements `delay_bars` scheduling; `nodes.py:evaluate_node` (lines ~280-300) applies position immediately to next return. Blocks phase advancement (simulation integrity).  
- Run artifacts for `20260412T013702Z-d73c98e2` missing from repo (all raw paths 404). Violates “persisted artifacts” gate.  
- Trade-count inconsistency flagged in query (report.md headline 43 vs run_metrics 87) unverifiable.

**Major**  
- Funding logic only in graph (`nodes.py:evaluate_node`) but absent from batch simulator; config lacks `funding_rate_per_8h`.  
- `optimize_node` referenced in `graph.py` but definition missing from traced `nodes.py` (file appears truncated).  
- Multi-timeframe features (`features.py`) partially traced; risk of non-causal as-of joins not ruled out.

**Minor**  
- Config fields `optuna_trials`, `walk_forward` declared but not exercised in main OOS run path.  
- Dev-plan mentions 1m data; runtime is 15m (config + query).  

**Nice-to-have**  
- SHAP rule derivation present but defensive-only.

# 6. Evidence From Current Run Artifacts
Artifacts for the assessed run_id are **not present** in the repository, preventing direct inspection. Using the query-provided summary as proxy evidence:  
- Held-out Sharpe ≈ -31.26, walk-forward mean Sharpe ≈ -26.37, total return ≈ -4.91%, max DD ≈ 5.15%. Strongly negative but consistent with a directional baseline under current market regime (research outcome, not defect).  
- Trade count mismatch (43 in report.md headline vs 87 in run_metrics.json) indicates reporting defect or post-processing bug in `persist_run_artifacts`.  
- Thresholds (long ≈ 0.52143, short ≈ 0.47944) and “no overfit warning” despite poor metrics suggest calibration succeeded but simulation realism is broken (timing mismatch explains unrealistic negative Sharpe).  
- OOS bars (199) match split_metadata intent; no evidence of leakage from code alone. Poor results are expected research outcome; inconsistency and missing files are implementation defects.

# 7. Hidden or Non-Obvious Issues
- `signal_delay_bars=1` is declared in config.yaml and used in calibration but **ignored** in LangGraph path.  
- `nodes.py:evaluate_node` funding logic references non-existent config key.  
- Repo .gitignore or CI likely excludes new run folders → artifacts never reach GitHub.  
- Naming `simulate_policy` vs runtime nodes misleads contributors into assuming identical semantics.  
- 15m-specific funding interval (32 bars) is hard-coded; fragile for timeframe changes.  
- `build_features` on full DF before split is safe only if target/features are strictly causal; any future multi-TF logic change could introduce silent leakage.

# 8. Test Coverage Assessment
**Well covered**: split metadata embargo logic (`test_pipeline.py`), basic feature build, risk clamping.  
**Weakly covered**: end-to-end timing semantics (no test asserts calibration vs graph return equivalence), walk-forward execution, artifact JSON consistency, multi-TF leakage, signal-delay round-trip.  
**10 highest-value additional tests to add next** (in priority order):  
1. Assert `simulate_policy` return series == graph OOS equity curve (same thresholds).  
2. Round-trip test: calibrate thresholds → run graph → verify exact same trades.  
3. Purge-gap walk-forward temporal validity test.  
4. Signal-delay=1 vs delay=0 equivalence on synthetic data.  
5. Funding interval enforcement in both simulators.  
6. Artifact JSON schema + trade_count cross-check.  
7. Multi-TF feature leakage test (future bar contamination).  
8. OOS cursor never sees validation data.  
9. Overfit-warning trigger on negative Sharpe.  
10. Regression test for run_metrics vs report.md headline consistency.

# 9. Phase-Gate Recommendation
**Do Not Advance**.  
Next phase (per dev-plan): “stable snapshot-based runs + reproducible held-out/walk-forward + benchmark regression tracking” before any fine-tuning gate.  
- **Must-fix before advancing**: timing mismatch, commit run artifacts, fix trade-count reporting.  
- **Should-fix soon after**: funding parity, full optimize_node implementation, multi-TF leakage audit.  
- **Safe to defer**: feature ablations, broader benchmarks.

# 10. Priority Action Plan
| Priority | Action | Why | Estimated effort | Blocks next phase? |
|----------|--------|-----|------------------|--------------------|
| 1 (Critical) | Align signal-delay logic between `evaluation.py:simulate_policy` and `nodes.py:evaluate_node` (add scheduling dict to graph path) | Simulation integrity broken | 4-6 hours | Yes |
| 2 (Critical) | Commit `artifacts/20260412T013702Z-d73c98e2/*` (or add to .gitignore exclusion + CI artifact upload) | Reproducibility gate | 1 hour | Yes |
| 3 (Major) | Fix trade-count reporting in `persist_run_artifacts` + add JSON consistency test | Metric integrity | 2 hours | Yes |
| 4 (Major) | Add funding logic to batch simulator matching graph | Consistency | 2 hours | No |
| 5 (Minor) | Implement missing `optimize_node` and wire walk-forward benchmark in main path | Plan compliance | 3 hours | No |

# 11. Detailed Fix Plan and Fine-Tuning Readiness Plan

## 11A. Detailed Fix Plan
**Fix 1: Signal-delay alignment**  
- **Objective**: Make calibration and runtime produce identical trade/return series for same thresholds.  
- **Exact files**: `src/agents/nodes.py` (add scheduling to evaluate_node), `src/agents/evaluation.py` (extract shared `apply_delayed_action` helper).  
- **Functions to rewrite**: `evaluate_node`, `simulate_policy`.  
- **Exact logic change**: Copy scheduled_actions dict pattern from simulate_policy into graph evaluate_node; use `idx + delay_bars` for action application.  
- **Why**: Current mismatch invalidates thresholds.  
- **Validation**: Add test asserting identical equity_curve for synthetic close/probs.  
- **Artifact changes expected**: run_metrics.json trade_count and sharpe now match report.md headline; benchmark_metrics.json stable.  
- **New tests**: “test_signal_delay_roundtrip”.  
- **Required before advancing**: Yes.

**Fix 2: Artifact persistence & commit**  
- **Objective**: Ensure every run_id folder is Git-tracked or CI-uploaded.  
- **Exact files**: `.gitignore` (remove artifacts/ exclusion or make run-specific), `run_mvp.py:persist_run_artifacts`.  
- **Functions**: `persist_run_artifacts` (add git-add step or CI hook).  
- **Exact logic change**: After write, run `git add artifacts/<run_id>` (non-blocking) or emit CI artifact.  
- **Why**: Gate requires persisted artifacts.  
- **Validation**: `ls artifacts/<run_id>` + re-run and confirm report.md contains 87 trades.  
- **Artifact changes**: All JSON/MD now in repo.  
- **New tests**: None (CI test).  
- **Required before advancing**: Yes.

**Fix 3: Trade-count reporting**  
- **Objective**: Eliminate headline vs JSON mismatch.  
- **Exact files**: `agents/artifacts.py` (or wherever report.md is built).  
- **Functions**: report builder.  
- **Exact logic change**: Use `len(trades)` from final_state consistently.  
- **Why**: Inconsistency breaks regression tracking.  
- **Validation**: Re-run → report.md headline == run_metrics.trade_count.  
- **Artifact changes**: Consistent numbers.  
- **New tests**: “test_artifact_trade_count_consistency”.  
- **Required before advancing**: Yes.

## 11B. Fine-Tuning Readiness Plan
Fine-tuning must **not** start now.  
Preconditions required before opening the fine-tuning gate (per docs/development-plan.md “Fine-Tuning Gate”):  
- Stable snapshot-based runs (≥3 historical windows persisted in repo).  
- Held-out reproducibility: same thresholds → identical equity_curve within 1e-6 across re-runs.  
- Walk-forward reproducibility: benchmark_metrics.json mean Sharpe stable ±0.05 across ≥5 folds.  
- Benchmark regression tracking: compare_benchmark_metrics test passing on every run.  
- Artifact completeness: every run_id must contain report.md, run_metrics.json, benchmark_metrics.json, split_metadata.json, strategy_params.json, optimization_events.json, trades/*.csv.  
- Metric consistency issues eliminated (trade_count, Sharpe match across files).  
- Leakage/timing risks closed (Fix 1 above + full multi-TF audit).  
- Minimum 5 historical windows persisted before considering fine-tuning.  
- Evidence justifying gate: CI pipeline showing <1% variance in held-out Sharpe across windows + no timing warnings.  

**Clear yes/no conclusion**: No – fine-tuning gate is not satisfied. Do not open any LoRA/RL work until the three must-fix items above are complete and ≥3 committed run artifacts exist.

# 12. Final Bottom Line
This codebase is **not** good enough to move forward. The timing mismatch between calibration and runtime, missing run artifacts, and reporting inconsistency are blocking engineering defects that must be resolved before any phase advance or fine-tuning discussion. Once fixed and artifacts are committed, the project can conditionally advance to a reproducible research baseline. Until then, it remains prototype wiring only.